import React from 'react'

type Props = {}

const Hero = (props: Props) => {
  return (
    <div className='w-[100vw] h-[85vh] bg-white text-primary grid justify-center items-center'>
      this is it
    </div>
  )
}

export default Hero